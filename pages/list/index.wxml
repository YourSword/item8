<!--index.wxml-->
<view class="container">
  <block wx:for-items="{{items}}">
    <view class="l-r l-c item">
      <view class="image">
        <image src="{{item.imgs[0]}}"/>
      </view>
      <view class="l_auto item-right">
        <view class="title"><text>{{item.title}}</text></view>
        
        <text class="c-1">我没开过</text>
      </view>
    </view>
  </block>
</view>
